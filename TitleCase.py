# Convert a sentence into title case.
# Each word in the string will start with an uppercase letter.
# This method automatically handles multiple words separated by spaces.

class Solution:
    def toTitle(self, s: str) -> str:
        return s.title()
