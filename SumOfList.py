# Sum of List
# - Given a list of numbers, find the sum of all elements.
# - You can use the built-in `sum()` function or loop through the list manually.

def sum_list(arr):
    total = 0
    for num in arr:
        total += num
    return total

