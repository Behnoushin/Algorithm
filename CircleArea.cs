// Circle Area
// - Given the radius of a circle, return its area.
// - Example: radius = 4 → area ≈ 50.27

using System;

class Solution
{
    public double CircleArea(double radius)
    {
        return Math.PI * radius * radius;
    }
}
